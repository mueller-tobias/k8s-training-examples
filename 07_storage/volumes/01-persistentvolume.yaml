apiVersion: v1
kind: PersistentVolume
metadata:
  name: persistentvolume-test
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 2Gi
########################################
  hostPath:
    path: /data/static
