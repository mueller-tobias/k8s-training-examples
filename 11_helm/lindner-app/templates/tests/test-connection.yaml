apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lindner-app.fullname" . }}-test-connection"
  labels:
    {{- include "lindner-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lindner-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
