apiVersion: v1
kind: Service
metadata:
  name: {{ include "bader.fullname" . }}
  labels:
    {{- include "bader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bader.selectorLabels" . | nindent 4 }}
