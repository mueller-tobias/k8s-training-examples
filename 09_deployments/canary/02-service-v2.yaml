apiVersion: v1
kind: Service
metadata:
  labels:
    run: demo-app-v2
  name: demo-app-v2
  namespace: rolling-updates
spec:
  ports:
    - port: 8080
      protocol: TCP
      name: web
  selector:
    run: demo-app-v2
  sessionAffinity: None
  type: ClusterIP
